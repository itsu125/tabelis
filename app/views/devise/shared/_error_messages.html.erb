<% if resource.errors.any? %>
  <div class="mb-6 p-4 rounded-xl border-2 border-red-500 bg-red-100 text-red-800 shadow-lg"
       id="error_explanation">
    
    <%# エラー件数と概要の表示 %>
    <strong class="block text-lg font-bold mb-3 text-red-900">
      <span class="text-xl mr-2">⚠️</span>
      入力内容に <%= resource.errors.count %> 件のエラーがあります。
    </strong>
    
    <%# エラーメッセージリスト %>
    <ul class="list-disc pl-5 space-y-2 text-sm">
      <% resource.errors.full_messages.each do |message| %>
        <li class="break-words"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>