<style>
    /* メッセージボックスのCSSは、fixed配置と中央寄せのため */
    #message-box {
        position: fixed;
        bottom: 1rem;
        left: 50%;
        transform: translateX(-50%);
        opacity: 0;
        pointer-events: none;
        transition: opacity 300ms;
        z-index: 50;
    }
</style>

<!-- メインコンテナ (モバイル画面を想定し、最大幅を制限) -->
<div id="app" class="max-w-md w-full min-h-screen mx-auto bg-white shadow-xl border-x border-t-border box-border text-t-dark">
    
    <!-- A. トップヒーローセクション -->
    <div class="py-12 px-6 text-center bg-t-light-header border-b-4 border-t-primary">
        <!-- 1. ロゴとキャッチコピー -->
        <h1 class="text-6xl font-extrabold tracking-tight text-t-primary mb-2 drop-shadow-md">
            <span class="text-7xl">🐿️</span> Tabelis
        </h1>
        <p class="text-lg text-t-text-mid font-medium mb-8">
            「いつか行きたい」を、あなただけのリストに。
        </p>

        <!-- 2. 新規登録・ログインボタン -->
        <div class="space-y-4 mt-12 mb-12 px-4">
            <!-- 新規登録ボタン (Primary) -->
            <%= link_to new_user_registration_path,
                    class: "block w-full py-4 text-lg font-bold text-white text-center rounded-xl transition ease-in-out duration-200
                           bg-t-primary shadow-[0_4px_0_0_#d8763a] active:shadow-[0_2px_0_0_#d8763a] active:translate-y-0.5" do %>
              新規登録
            <% end %>
            <!-- ログインボタン (Secondary) -->
            <%= link_to new_user_session_path,
                    class: "block w-full py-4 text-lg font-bold rounded-xl transition ease-in-out duration-200 
                           bg-t-secondary text-t-dark shadow-[0_4px_0_0_#e09f58] active:shadow-[0_2px_0_0_#e09f58] active:translate-y-0.5" do %>
              ログイン
            <% end %>
        </div>
    </div>

    <main class="p-4">
      <!-- E. コンテンツエリア (デモカードリスト) -->
      <h2 class="text-2xl font-bold text-t-text-mid mb-4 text-center 
                  border-b border-dashed border-t-border pb-2">
        👀 デモリストを見る
      </h2>
      <div class="space-y-4">

        <% demos = [
          {
            name: "木漏れ日ベーカリー",
            image: "https://placehold.co/300x300/f0a256/442e18?text=Cafe",
            category: "カフェ",
            category_class: "bg-t-secondary text-t-dark border border-t-border",
            rating: 4,
            address: "東京・代々木",
            tags: [
              { name: "友達と", class: "bg-tag-friends text-white" }
            ]
          },
          {
            name: "窯焼きピッツァ IL FORNO",
            image: "https://placehold.co/300x300/ffc48d/442e18?text=Lunch",
            category: "イタリアン",
            category_class: "bg-t-secondary text-t-dark border border-t-border",
            rating: 5,
            address: "大阪・難波",
            tags: [
              { name: "ランチ", class: "bg-tag-lunch text-white" }
            ]
          },
          {
            name: "隠れ家スイーツ Atelier",
            image: "https://placehold.co/300x300/fff8e1/442e18?text=Sweets",
            category: "スイーツ",
            category_class: "bg-t-secondary text-t-dark border border-t-border",
            rating: 3,
            address: "東京・恵比寿",
            tags: [
              { name: "記念日に", class: "bg-tag-memory text-white" }
              ]
          }
        ] %>

        <% demos.each do |demo| %>
          <%= render "demo_card", demo: demo %>
        <% end %>
      </div>